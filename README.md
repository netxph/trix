# Dart Tracker

Mobile-first React and TypeScript PWA built with Bun.

```powershell
bun install
bun run dev
```

## Checks

```powershell
bun run typecheck
bun run lint
bun test
bun run build
```

The production PWA is written to `dist/` with a generated offline service worker.
